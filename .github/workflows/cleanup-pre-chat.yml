name: Cleanup Pre Chat

on:
  schedule:
    # 每小時執行一次（UTC 時間）
    - cron: '0 * * * *'
  # 也可以手動觸發
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Run cleanup
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          API_URL: ${{ secrets.API_URL || 'https://your-domain.com' }}
        run: |
          echo "🧹 開始清理過期的預聊房間..."
          
          # 確保 API_URL 沒有尾隨斜線
          API_URL=$(echo "${API_URL}" | sed 's|/$||')
          FULL_URL="${API_URL}/api/internal/cleanup-pre-chat"
          
          echo "🔍 除錯資訊："
          echo "API_URL: ${FULL_URL}"
          echo "CRON_SECRET 長度: ${#CRON_SECRET} 字元"
          echo ""
          
          # 使用 -L 參數跟隨重定向，不使用 -f 參數以便捕獲錯誤訊息
          response=$(curl -s -L -w "\n%{http_code}" -X POST "${FULL_URL}" \
            -H "Authorization: Bearer ${CRON_SECRET}" \
            -H "Content-Type: application/json" \
            2>&1)
          
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          
          echo "HTTP Status Code: ${http_code}"
          echo "Response body:"
          echo "$body"
          echo ""
          
          if [ "$http_code" != "200" ]; then
            echo "❌ 清理失敗 (HTTP ${http_code})"
            exit 1
          else
            echo "✅ 清理成功"
            echo "$body"
          fi

